<h1><%= @instructor.name %></h1>
<ul>
  <% @instructor.students.each do |student| %>
    <li><%= student.name %></li>
  <% end %>
</ul>
<p>Average Age of <%= @instructor.name %>'s
  Students: <%= @instructor.avg_student_age.round(0) %></p>
